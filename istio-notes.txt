kubectl exec -it istio-ingressgateway-647966f79b-s7qbh  -c istio-proxy -n istio-system -- curl -s localhost:15000/config_dump | grep format -A 10


meshConfig:
  enablePrometheusMerge: true
  enableTracing: false
  accessLogFile: /dev/stdout
  accessLogEncoding: JSON
  extensionProviders:
      - name: envoy-custom-access-log
        envoyFileAccessLog:
          path: /dev/stdout
          logFormat:
            labels:
              start_time: "%START_TIME%"
              correlationId: "%REQ(X-Amzn-Trace-Id)%"
              compositeUserId: "%REQ(x-auth-subject)%"
              source_host: "%HOSTNAME%"
              upstream_host: "%UPSTREAM_HOST%"
              remote_address: "%DOWNSTREAM_DIRECT_REMOTE_ADDRESS%"
              user_agent: "%REQ(USER-AGENT)%"
              host: "%REQ(:AUTHORITY)%"
              protocol: "%PROTOCOL%"
              bytes_rcvd: "%BYTES_RECEIVED%"
              request:
                request_method: "%REQ(:METHOD)%"
                request_path: "%REQ(X-ENVOY-ORIGINAL-PATH?:PATH)%"
                request_time: "%DURATION%"
                request_xff: "%REQ(X-FORWARDED-FOR)%"
                request_id: "%REQ(X-REQUEST-ID)%"
              response:
                response_code: "%RESPONSE_CODE%"
                response_code_details: "%RESPONSE_CODE_DETAILS%"
                response_bytes_sent: "%BYTES_SENT%"
                response_flags: "%RESPONSE_FLAGS%"
                response_time: "%RESPONSE_DURATION%"
                response_upstream_time: "%RESP(X-ENVOY-UPSTREAM-SERVICE-TIME)%"
